-- Seed data for local development
-- This runs after migrations when using `supabase db reset`

-- Languages are inserted in the migration file
-- Add additional seed data here as needed

-- Example: Additional languages (uncomment to enable)
-- insert into public.languages (code, name, is_active) values
--   ('fr', 'Français', true),
--   ('de', 'Deutsch', true),
--   ('pt', 'Português', true)
-- on conflict (code) do nothing;
